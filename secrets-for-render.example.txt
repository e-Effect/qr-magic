# 手順:
# 1. このファイルをコピーして、同じフォルダに「secrets-for-render.txt」という名前で保存する
#    （example のままでは動きません。名前を secrets-for-render.txt にすること）
# 2. メモ帳で開き、下の = の右側だけ埋める（長い鍵はブラウザでコピーしてここに貼る）
# 3. 保存してから「render-supabase-from-notepad.cmd」をダブルクリック
#    （黒い画面に鍵を貼らない。閉じる問題が出にくいです）
#
# SUPABASE_URL はあなたのプロジェクトなら次の形でよいです（/rest/v1/ は付けない）:
SUPABASE_URL=https://pftlyqqthfdhyzywzwfo.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
UPSTASH_STATE_KEY=qr-magic:app-state
DATABASE_URL=
RENDER_API_KEY=
RENDER_SERVICE_ID=
RENDER_POSTGRES_ID=
